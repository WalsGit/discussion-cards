import Page from 'flarum/components/Page';

export default class TagsSettings extends Page {
  view() {
    return (
      <div>
        <h2>Custom settings per tag</h2>
        <p>testing...</p>
      </div>
    );
  }
}